import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fixup.app',
  appName: 'FixUp',
  webDir: 'dist/fixup-frontend/browser'
};

export default config;
